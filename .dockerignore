# Dependencies & build output (rebuilt inside the image)
node_modules
dist
build
generated/prisma
coverage
.nyc_output

# Local infra / tooling
docker-compose.yml
Dockerfile
.dockerignore
.git
.gitignore
.github

# Env & secrets — provided at runtime, never baked into the image
.env
.env.*
bm-firebase-push.json

# Local file storage (mounted as a volume in prod)
uploads

# Editor / OS cruft
.vscode
.idea
.DS_Store
*.log

# Tests & misc
test
*.tsbuildinfo
README.md
